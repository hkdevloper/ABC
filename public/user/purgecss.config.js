module.exports = {
    content: ['./**/*.html'], // Specify the HTML files to analyze using globs
    css: ['./style/*.css'], // Specify the CSS files to optimize using globs
    // Other options, such as extractors or safelist, can be configured here
};
