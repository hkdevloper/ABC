import './bootstrap';
import { Datepicker, Tab, Datatable,Dropdown, Ripple, initTE } from "tw-elements";

// Initiate all components at once
initTE({
    Datepicker,
    Tab,
    Datatable,
    Dropdown,
    Ripple
});
